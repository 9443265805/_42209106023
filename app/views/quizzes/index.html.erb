<%= link_to "New Quiz", new_quiz_path , class: "button radius" %> 
<h1>Listing Quizzes </h1>
<table>
<tr>
<th>Quiz Name</th>
<th>Category</th>
<th>Starttime</th>
<th>Endtime</th>
<th>Date</th>
<% if current_user.role=="Admin" %>
<th>Manage Questions</th>
<% end %>
<th>Manage Students</th>
<th>Questions Assigned</th>
<th>Status 
</tr>
<% @quizzes.each do |f| %>

	<tr>
		<td><%= f.quizname %></td>
		<td><%= f.category %></td>
		<td><%= f.starttime.strftime('%H:%M') %></td>
		<td><%= f.endtime.strftime('%H:%M') %></td>
		<td><%= f.date.strftime("%d/%m/%Y") %></td>
		<% if current_user.role=="Admin" %>
		<td><%= link_to 'Manage Questions' , admin_assignquestions_path(quiz_id:f.id)  , class: "button radius" %></td>
		<% end %>
		<td><%= link_to 'Manage Students' , teacher_assignstudents_path(quiz_id:f.id) , class: "button radius" %></td>
		<td><%= f.count %></td>
		<td><%= f.status %></td>
		<td><%= link_to 'Show', f  , class: "button radius"%></td>
		<% if current_user.role=="Admin" %>
		<td><%= link_to 'Edit' ,edit_quiz_path(f) , class: "button radius"%></td>
		<td><%= link_to 'Delete', f , method: :Delete ,date: {confrim:"Are you sure?" } , class: "button radius" %></td>
		<% end %>
	</tr>
	<% end %>
</table>
<div class="apple_pagination">
<%= will_paginate @quizzes %>
</div>
